plugins {
	id "java"
    id "maven-publish"
}

dependencies {
    compile(
        'com.spotify:docker-client:3.1.0'
    )
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId "tocker-core"
            from components.java
        }
    }
}
